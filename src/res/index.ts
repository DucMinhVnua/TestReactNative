import colors from './colors';
import fonts from './fonts';
import images from './images';
import palette from './palette';
import strings from './strings';

const CORE_RES = {
  colors,
  fonts,
  images,
  palette,
  strings,
};

export default CORE_RES;
